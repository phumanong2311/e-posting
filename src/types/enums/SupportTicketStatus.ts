export enum SupportTicketStatus {
  OPEN = "openTickets",
  CLOSED = "closedTickets",
  ALL = "",
}

export enum TicketProcessStatus {
  OPEN = "open",
  IN_REVIEW = "inReview",
  CANCELED = "canceled",
  ON_HOLD = "onHold",
  RESOLVED = "resolved",
}

export enum TicketPriorityLevel {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
  CRITICAL = "critical",
}
